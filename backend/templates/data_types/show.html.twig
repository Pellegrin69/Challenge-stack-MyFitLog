{% extends 'base.html.twig' %}

{% block title %}DataTypes{% endblock %}

{% block body %}
    <h1>DataTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ data_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ data_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_data_types_index') }}">back to list</a>

    <a href="{{ path('app_data_types_edit', {'id': data_type.id}) }}">edit</a>

    {{ include('data_types/_delete_form.html.twig') }}
{% endblock %}
