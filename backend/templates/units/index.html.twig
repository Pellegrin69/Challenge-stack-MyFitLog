{% extends 'base.html.twig' %}

{% block title %}Units index{% endblock %}

{% block body %}
    <h1>Units index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Coefficient</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unit in units %}
            <tr>
                <td>{{ unit.id }}</td>
                <td>{{ unit.name }}</td>
                <td>{{ unit.coefficient }}</td>
                <td>
                    <a href="{{ path('app_units_show', {'id': unit.id}) }}">show</a>
                    <a href="{{ path('app_units_edit', {'id': unit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_units_new') }}">Create new</a>
{% endblock %}
